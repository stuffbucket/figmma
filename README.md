# figmma

MCP server with real-time observability dashboard.

## Quick Start

1. `make pack` — builds `figmma-1.0.0.tgz`
2. `npm install /path/to/figmma-1.0.0.tgz` — run this in any project folder
3. `claude` — MCP tools are available immediately
